---
type: concept
x-llmwiki:
  schemaVersion: "0.1"
  contentHash: eacfa678009677b846d6bd62c853ff17c4f3723fd0338367079c91a786a0be54
  pageDirectory: concepts
  sources:
    - opentelemetry-export-databricks-on-aws.md
  confidence: 0.95
  provenanceState: extracted
  freshnessStatus: unverified
  aliases:
    - mlflow-opentelemetry-compatibility
    - MOC
  citations:
    - file: opentelemetry-export-databricks-on-aws.md
title: MLflow OpenTelemetry Compatibility
description: MLflow traces conform to the OpenTelemetry trace specification, enabling export to any OpenTelemetry-compatible observability solution.
tags:
  - observability
  - MLflow
  - OpenTelemetry
timestamp: "2026-06-19T19:51:00.540Z"
---

# MLflow OpenTelemetry Compatibility

**MLflow OpenTelemetry Compatibility** refers to MLflow's ability to generate traces that conform to the [OpenTelemetry trace specification](https://opentelemetry.io/docs/specs/otel/trace/api/#span), enabling export to various observability solutions that support OpenTelemetry. This compatibility allows teams to integrate MLflow's machine learning experiment tracking with existing observability infrastructure. ^[opentelemetry-export-databricks-on-aws.md]

## Overview

Traces generated by [MLflow](/concepts/mlflow.md) are compatible with OpenTelemetry span semantics. This means MLflow traces can be sent to OpenTelemetry Collectors and from there to any compatible observability backend, such as Datadog, Grafana, Jaeger, or Prometheus. ^[opentelemetry-export-databricks-on-aws.md]

## Export Modes

MLflow supports three export modes for traces: ^[opentelemetry-export-databricks-on-aws.md]

| Mode | Description |
|------|-------------|
| **MLflow tracking only (Default)** | Traces are sent only to the MLflow Tracking Server. |
| **OpenTelemetry only** | Traces are sent only to an OpenTelemetry Collector. |
| **Dual export** | Traces are sent to both MLflow Tracking and an OpenTelemetry Collector simultaneously. |

## OpenTelemetry-Only Export

To export traces *only* to an OpenTelemetry Collector instead of the MLflow Tracking Server, set the `OTEL_EXPORTER_OTLP_ENDPOINT` or `OTEL_EXPORTER_OTLP_TRACES_ENDPOINT` environment variable to the target URL of the OpenTelemetry Collector *before starting any trace*. ^[opentelemetry-export-databricks-on-aws.md]

```python
import mlflow
import os

# Set the endpoint of the OpenTelemetry Collector
os.environ["OTEL_EXPORTER_OTLP_TRACES_ENDPOINT"] = "http://localhost:4317/v1/traces"
# Optionally, set the service name to group traces
os.environ["OTEL_SERVICE_NAME"] = "<your-service-name>"

# Trace will be exported ONLY to the OTel collector
with mlflow.start_span(name="foo") as span:
    span.set_inputs({"a": 1})
    span.set_outputs({"b": 2})
```

^[opentelemetry-export-databricks-on-aws.md]

## Dual Export (MLflow + OpenTelemetry)

Dual export enables sending the same trace data to both Databricks MLflow and another OpenTelemetry service simultaneously. This is useful for organizations that want to maintain MLflow's tracking capabilities while also integrating with existing observability infrastructure. ^[opentelemetry-export-databricks-on-aws.md]

### Enable Dual Export

Set the `MLFLOW_ENABLE_DUAL_EXPORT` environment variable to `"true"` along with your OpenTelemetry configuration: ^[opentelemetry-export-databricks-on-aws.md]

```python
import mlflow
import os

# Enable dual export mode
os.environ["MLFLOW_ENABLE_DUAL_EXPORT"] = "true"
# Configure OpenTelemetry Collector endpoint
os.environ["OTEL_EXPORTER_OTLP_TRACES_ENDPOINT"] = "http://localhost:4317/v1/traces"
os.environ["OTEL_SERVICE_NAME"] = "my-ml-service"
# Configure MLflow tracking URI to Databricks
mlflow.set_tracking_uri("databricks")

# Traces will be exported to BOTH MLflow and the OTel collector
with mlflow.start_span(name="dual_export_example") as span:
    span.set_inputs({"model": "gpt-4", "prompt": "Hello world"})
    # Your ML workflow here
    result = "Generated response"
    span.set_outputs({"response": result})
    span.set_attributes({"token_count": 15})
```

^[opentelemetry-export-databricks-on-aws.md]

## Metrics Export

MLflow can export OpenTelemetry metrics when a metrics endpoint is configured. This allows monitoring of span durations and other trace-related metrics in compatible monitoring systems. ^[opentelemetry-export-databricks-on-aws.md]

### Enable Metrics Export

To export metrics to an OpenTelemetry Collector, set the following environment variables: ^[opentelemetry-export-databricks-on-aws.md]

```python
import os

# Enable metrics export
os.environ["OTEL_METRICS_EXPORTER"] = "otlp"
os.environ["OTEL_EXPORTER_OTLP_METRICS_ENDPOINT"] = "http://localhost:4317"
# Optional: Configure metric export interval (in milliseconds)
os.environ["OTEL_METRIC_EXPORT_INTERVAL"] = "60000"  # Export every 60 seconds
```

For a complete list of metrics that MLflow exports, see the MLflow Exported Metrics documentation. ^[opentelemetry-export-databricks-on-aws.md]

## OpenTelemetry Collector Configuration

MLflow uses the standard OTLP Exporter for exporting traces to OpenTelemetry Collector instances. All [standard OpenTelemetry exporter configurations](https://opentelemetry.io/docs/languages/sdk-configuration/otlp-exporter/) are supported. ^[opentelemetry-export-databricks-on-aws.md]

The following example configures the OTLP Exporter to use HTTP protocol instead of the default gRPC and sets custom headers: ^[opentelemetry-export-databricks-on-aws.md]

```bash
export OTEL_EXPORTER_OTLP_TRACES_ENDPOINT="http://localhost:4317/v1/traces"
export OTEL_EXPORTER_OTLP_TRACES_PROTOCOL="http/protobuf"
export OTEL_EXPORTER_OTLP_TRACES_HEADERS="api_key=12345"
```

## Related Concepts

- [MLflow Tracing](/concepts/mlflow-tracing.md) — How MLflow generates OpenTelemetry-compatible traces
- Span Instrumentation — Adding custom spans to enrich OpenTelemetry exports
- Production Tracing — Using exported traces to monitor production ML applications
- OpenTelemetry Collector — The intermediary service for receiving and forwarding telemetry data
- Observability Backends — Systems like Datadog, Grafana, and Jaeger that consume OpenTelemetry data

## Sources

- opentelemetry-export-databricks-on-aws.md

# Citations

1. [opentelemetry-export-databricks-on-aws.md](/references/opentelemetry-export-databricks-on-aws-d0c0ee4c.md)
